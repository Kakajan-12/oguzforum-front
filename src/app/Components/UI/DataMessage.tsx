"use client";

const DataMessage = () => {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <p className="text-mainBlue text-xl font-semibold">No events found</p>
    </div>
  );
};

export default DataMessage;
