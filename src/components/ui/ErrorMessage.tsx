"use client";

const ErrorMessage = () => {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <p className="text-mainBlue text-xl font-semibold">Error loading data</p>
    </div>
  );
};

export default ErrorMessage;
